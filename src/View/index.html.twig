{% extends 'base.html.twig' %}

{% block content %}

    {% for product in products %}

        <div class="col-12 ">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{ product }}</h5>
                </div>
            </div>
        </div>

    {% endfor %}
{% endblock %}